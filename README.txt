To upload a new table (such as processor), you need to load the new data into
the processor table and also need to citysearch_processor also. This citysearch_processor 
table has the distance from a city to the processor.
